// import { CurrentAdmin } from '../interface/admin.interface.js';

// export const adminAccessible = {
//   isAccessible: ({ currentAdmin }: CurrentAdmin): boolean => currentAdmin.role === 'ADMIN',
// };
